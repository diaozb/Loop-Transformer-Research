trainer:
  name: fixed_loop
  train_steps: 100001
  batch_size: 64
  eval_batch_size: 512
  eval_every: 1000
  ema: false
optimizer:
  name: adamw
  learning_rate: 0.0001
  weight_decay: 0.0
  grad_clip_norm: 1.0
  lr_schedule: cosine_after_curriculum
